<!DOCTYPE html>
<html>
    <head>
        <% include ./meta-resources.ejs %>
        <link rel='stylesheet' href='/stylesheets/pg-sign-up.css' />
        <link rel='stylesheet' href='/stylesheets/pg-login.css' />
    </head>
    <body>
        <% include ./header.ejs %>
        
        <div class="mk-account-wrap mk-account-font-color">
            <% if (page === 'SIGN_UP') { %>
                <% include ./form-sign-up.ejs %>
            <% } else if (page === 'LOGIN') { %>
                <% include ./form-login.ejs %>
            <% } %>
        </div>
        <% include ./lib-url.ejs %>
    </body>
</html>
